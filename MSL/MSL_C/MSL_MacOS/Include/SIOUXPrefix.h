/*  Metrowerks Standard Library  *//*  $Date: 1999/01/22 23:42:11 $  *  $Revision: 1.2 $  *  $NoKeywords: $  * *		Copyright © 1995-1999 Metrowerks, Inc. *		All rights reserved. */ /* This file is only required when using the SIOUX source code to   compile, e.g. building a new library. It loads the proper headers   for preprocessor #defines (namely __MSL__) enabling us to compile   SIOUX for either MSL or Plauger compatibility.      If there is a mixing of the types of libraries, your end project   _will_ crash.      If your SIOUX lib is compiled properly (i.e. you're using an   MSL-compatible lib in an MSL project), you do not need to include   this file--just SIOUX.h if you want to change SIOUX settings.      --pcg*//* uncomment the following line if using the "Plauger"   (aka "Plum-Hall" or "Obsolete ANSI") libs *//* #define __USING_PLAUGER__ /* */#ifndef __USING_PLAUGER__	#if __MWERKS__ >= 0x1800		#include "ansi_prefix.mac.h"	#else		#include "ansi_parms.h"	#endif#else	#include <MacHeaders.h>#endif#include <SIOUX.h>